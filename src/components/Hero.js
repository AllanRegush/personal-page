import React from "react";
import styled from 'styled-components';

const StyledDiv = styled.div`
    display: flex;
    align-items: center;
`;


const Hero = () => (
    <StyledDiv>
        
    </StyledDiv>
);

export default Hero